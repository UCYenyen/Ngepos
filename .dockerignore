node_modules
.next
.git
.github
.vscode
.idea
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Local env — never copy secrets into the image; they come from the VPS .env at runtime
.env
.env.*
!.env.example

# Tests, coverage, and editor artifacts
coverage
*.log

# Docs and design references (not needed in the runtime image)
design_handoff_ngepos
docs
*.md
!README.md

# Supabase CLI temp + local
supabase/.temp
supabase/.branches
